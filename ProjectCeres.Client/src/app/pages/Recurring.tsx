import { PagePlaceholder } from '../components/PagePlaceholder';

export function Recurring() {
  return (
    <PagePlaceholder
      title="Recurring Transactions"
      description="The Recurring Transactions page will land in a follow-up plan."
    />
  );
}
