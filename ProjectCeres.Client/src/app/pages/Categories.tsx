import { PagePlaceholder } from '../components/PagePlaceholder';

export function Categories() {
  return (
    <PagePlaceholder
      title="Categories"
      description="The Categories page will land in a follow-up plan."
    />
  );
}
