namespace ProjectCeres.Models;

/// <summary>User-declared urgency. Advisory only — nothing in the app routes on it yet.</summary>
public enum SupportTicketPriority
{
    Low = 0,
    Normal = 1,
    High = 2,
    Urgent = 3,
}
