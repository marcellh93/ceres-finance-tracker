@model TransactionCreateViewModel
@{
    ViewData["Title"] = "Confirm Reminder";
    var reminder = ViewBag.Reminder as RecurringTransaction;
}

<h1>Confirm: @reminder?.Name</h1>
<p class="form-hint">Review and adjust the details before recording this transaction.</p>

<form asp-action="Confirm" asp-route-id="@reminder?.Id" method="post">
    <div asp-validation-summary="ModelOnly" class="alert alert-error"></div>

    <input type="hidden" asp-for="AccountId" />
    <input type="hidden" asp-for="CategoryId" />
    <input type="hidden" asp-for="TransactionType" />

    <div class="form-group">
        <label asp-for="Date"></label>
        <input asp-for="Date" type="date" class="form-control" />
        <span asp-validation-for="Date" class="field-error"></span>
    </div>

    <div class="form-group">
        <label asp-for="Amount"></label>
        <input type="text" id="Amount" name="Amount"
               value="@NumberFormatHelper.FormatInputValue(Model.Amount, ViewData["NumberFormat"] as string)"
               class="form-control" />
        <span asp-validation-for="Amount" class="field-error"></span>
    </div>

    <div class="form-group">
        <label asp-for="Description"></label>
        <input asp-for="Description" class="form-control" placeholder="@reminder?.Name" />
    </div>

    @if (reminder?.ReminderBehaviour == ProjectCeres.Models.ReminderBehaviour.ManualDate)
    {
        <div class="form-group">
            <label for="nextDueDate">Next Due Date</label>
            <input type="date" id="nextDueDate" name="nextDueDate" class="form-control" required />
            <small>Required — set the next due date for this reminder.</small>
        </div>
    }

    <div class="form-actions">
        <button type="submit" class="btn btn-primary inline-flex items-center gap-1.5">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
            Confirm &amp; Record
        </button>
        <a asp-action="Index" class="btn btn-secondary inline-flex items-center gap-1.5">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
            Cancel
        </a>
    </div>
</form>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}
