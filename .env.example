# Database — Phase 1 (local, Homebrew PostgreSQL, no password needed)
CONNECTION_STRING=Host=localhost;Database=finance_tracker;Username=postgres;

# Database — Phase 3 (hosted managed PostgreSQL, e.g. Supabase, Railway, Neon)
# CONNECTION_STRING=Host=your-host;Database=finance_tracker;Username=your_username;Password=your_password;SslMode=Require;

# App
ASPNETCORE_ENVIRONMENT=Development
